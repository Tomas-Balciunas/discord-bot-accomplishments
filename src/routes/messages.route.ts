// import express from 'express';
// import * as controller from '../modules/messages/controller';
// import { validateBody } from '@/utils/middleware/middleware';
// import { bodySchema } from '@/modules/messages/schema';
// import { tryCatch } from '@/utils';

// const router = express.Router();

// router
//   .route('/')
//   .get(tryCatch(controller.selectAllMessages))
//   .post(validateBody(bodySchema), tryCatch(controller.sendDiscordMessage));

// export default router;
