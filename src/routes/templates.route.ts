// import express from 'express';
// import * as controller from '../modules/templates/controller';
// import { tryCatch } from '@/utils';

// const router = express.Router();

// router.route('/rand').get(tryCatch(controller.selectTemplateRandom));

// router
//   .route('/')
//   .get(tryCatch(controller.selectAllTemplates))
//   .post(tryCatch(controller.createTemplate));

// router
//   .route('/:id')
//   .get(tryCatch(controller.selectTemplateById))
//   .patch(tryCatch(controller.updateTemplate))
//   .delete(tryCatch(controller.deleteTemplate));

// export default router;
