// import express from 'express';
// import * as controller from '../modules/sprints/controller';
// import { tryCatch } from '@/utils';

// const router = express.Router();

// router
//   .route('/')
//     .get(tryCatch(controller.selectAllSprints))
//     .post(tryCatch(controller.createSprint));

// router
//   .route('/:id')
//     .get(tryCatch(controller.selectSprintById))
//     .patch(tryCatch(controller.updateSprint))
//     .delete(tryCatch(controller.deleteSprint));

// export default router;
